Just change path directory(src) in "start" script in "package.json" file to whatever path you want to run using nodemon.

"start": "nodemon -x \"cls && node\" ./Queue/Queue" : change only "./Queue/Queue" part.

Enjoy Learning DS!!!
